{
  "lexicon": 1,
  "id": "app.changala.globalview.markAllRead",
  "description": "Mark all notifications as read for the authenticated user.",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Mark all notifications as read for the authenticated user.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "properties": {}
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["markedCount"],
          "properties": {
            "markedCount": {
              "type": "integer",
              "description": "Number of notifications that were marked as read."
            }
          }
        }
      }
    }
  }
}
